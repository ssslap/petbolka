# 🐾 ВетКлиника «Здоровый питомец»

Веб-сайт ветеринарной клиники, разработанный на FastAPI с возможностью развертывания на GitHub Pages.

## 📋 Описание

Современный, адаптивный веб-сайт для ветеринарной клиники с полным функционалом:

- **Главная страница** — информация о клинике, преимущества, популярные услуги и врачи
- **Услуги** — полный каталог ветеринарных услуг с ценами
- **Врачи** — информация о специалистах клиники
- **Контакты** — контактная информация и форма обратной связи
- **Запись на прием** — онлайн-форма для записи на прием

## 🛠 Технологии

- **Backend:** Python 3.9+, FastAPI
- **Frontend:** HTML5, CSS3, JavaScript
- **Шаблонизатор:** Jinja2
- **Стилизация:** Адаптивный CSS с использованием CSS Grid и Flexbox

## 📁 Структура проекта

```
petttttttttttttt/
├── app/
│   ├── main.py           # Основной файл FastAPI приложения
│   ├── models.py         # Модели данных (Doctor, Service)
│   ├── routes.py         # Маршруты и эндпоинты
│   └── templates/        # HTML шаблоны
│       ├── base.html
│       ├── index.html
│       ├── services.html
│       ├── doctors.html
│       ├── contact.html
│       └── appointment.html
├── static/
│   ├── css/
│   │   └── style.css     # Стили
│   ├── js/
│   │   └── script.js     # JavaScript
│   └── images/           # Изображения
├── docs/                 # Статические файлы для GitHub Pages
├── generate_static.py    # Скрипт генерации статических файлов
├── requirements.txt      # Python зависимости
├── .gitignore
└── README.md
```

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/vet-clinic.git
   cd vet-clinic
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   # или
   venv\Scripts\activate     # Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите сервер разработки:**
   ```bash
   uvicorn app.main:app --reload
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:8000
   ```

### Генерация статических файлов для GitHub Pages

```bash
python generate_static.py
```

Статические файлы будут созданы в папке `docs/`.

## 📦 Развертывание на GitHub Pages

1. Сгенерируйте статические файлы:
   ```bash
   python generate_static.py
   ```

2. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Update static files"
   git push
   ```

3. В настройках репозитория GitHub:
   - Перейдите в **Settings** → **Pages**
   - В разделе **Source** выберите **Deploy from a branch**
   - Выберите ветку `main` и папку `/docs`
   - Сохраните настройки

Сайт будет доступен по адресу: `https://your-username.github.io/vet-clinic/`

## 🔧 API Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/` | Главная страница |
| GET | `/services` | Страница услуг |
| GET | `/doctors` | Страница врачей |
| GET | `/contact` | Страница контактов |
| GET | `/appointment` | Страница записи на прием |
| GET | `/api/doctors` | JSON список врачей |
| GET | `/api/services` | JSON список услуг |
| GET | `/api/clinic` | JSON информация о клинике |
| POST | `/api/contact` | Отправка формы обратной связи |
| POST | `/api/appointment` | Запись на прием |

## 🎨 Особенности дизайна

- ✅ Адаптивный дизайн для всех устройств
- ✅ Современный и чистый интерфейс
- ✅ Анимации при прокрутке
- ✅ Оптимизация для SEO
- ✅ Быстрая загрузка страниц
- ✅ Доступность (WCAG)

## 📝 Лицензия

MIT License

## 👥 Авторы

Разработано с ❤️ для здоровья ваших питомцев
